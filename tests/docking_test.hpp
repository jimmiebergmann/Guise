#include "test.hpp"
#include "guise/control/dockable.hpp"
#include "guise/control/button.hpp"

using namespace Guise;

TEST(Docking, Dockable)
{
    {

        /*class Foo
        {};

        auto button1 = Button::create();

        auto test = Dockable<Foo>::create();

        test->foo();
        auto type = test->getType();

        auto lol = std::is_base_of<Control, Foo>::value;

        std::cout << (int)type << lol << std::endl;*/
       
    }
}
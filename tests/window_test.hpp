#include "test.hpp"
#include "guise/context.hpp"
#include "guise/appWindow.hpp"

using namespace Guise;

TEST(Window, Window)
{
   // Context::foo();
   // auto context = Context::create();
    //auto window = Window::create(context, L"Test", { 100, 100});

   /* while (1)
    {
        Window::update();
    }*/


}